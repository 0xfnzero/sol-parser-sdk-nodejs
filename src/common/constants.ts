/**
 * 与 Rust `sol-parser-sdk/src/common/constants.rs` 对齐的流处理默认值。
 */
export const DEFAULT_CONNECT_TIMEOUT = 10;
export const DEFAULT_REQUEST_TIMEOUT = 60;
export const DEFAULT_CHANNEL_SIZE = 1000;
export const DEFAULT_MAX_DECODING_MESSAGE_SIZE = 1024 * 1024 * 10;

export const DEFAULT_METRICS_WINDOW_SECONDS = 5;
export const DEFAULT_METRICS_PRINT_INTERVAL_SECONDS = 10;
export const SLOW_PROCESSING_THRESHOLD_US = 3000;
